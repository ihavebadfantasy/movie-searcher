import { connect } from 'react-redux';
import SearchResults from '../search/SearchResults';
import SearchInput from '../forms/SearchInput';

const MultiSearch = () => {
  return (
    <div></div>
  );
}

const mapStateToProps = () => {

}

export default connect(null, null)(MultiSearch);
